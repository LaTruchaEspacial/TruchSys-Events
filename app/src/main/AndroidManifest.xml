<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tfg.azafatasapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light"> <!-- Asegúrate de usar un tema AppCompat -->

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.register.RegisterActivity" />
        <activity android:name=".ui.login.LoginActivity" />
        <activity android:name=".ui.home.HomeActivity" />
        <activity android:name=".ui.profile.PerfilActivity"/>
        <activity android:name=".Admin.MainActivityAdmin" />
        <activity android:name=".ui.admins.WorksAdminActivity" />
        <activity android:name=".ui.users.WorksUserActivity" />
        <activity android:name=".ui.users.MessageUserActivity" />
        <activity android:name=".ui.admins.MessageAdminActivity" />


    </application>

</manifest>
